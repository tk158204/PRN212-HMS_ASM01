<UserControl x:Class="TranTuanKietWPF.Views.RoomManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:TranTuanKietWPF.ViewModels">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}"
                Margin="0,0,0,20">
            <StackPanel Margin="20">
                <Label Content="Room Management" 
                     Style="{StaticResource HeadingLabel}"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,10"/>
                <Label Content="Manage hotel rooms, room types, and room availability" 
                     Style="{StaticResource ModernLabel}"
                     Foreground="{StaticResource DarkGrayColor}"
                     HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}"
                Margin="0,0,0,20">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Grid.Column="0" 
                          Orientation="Horizontal" 
                          VerticalAlignment="Center">
                    <TextBox x:Name="txtSearch" 
                           Style="{StaticResource ModernTextBox}"
                           Width="300"
                           Margin="0,0,10,0"
                           Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                           VerticalAlignment="Center"/>
                    <Button Content="Search" 
                          Style="{StaticResource ModernButton}"
                          Command="{Binding SearchCommand}"
                          VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Add Room Button -->
                <Button Grid.Column="1" 
                      Content="Add Room" 
                      Style="{StaticResource SuccessButton}"
                      Command="{Binding AddCommand}"/>
            </Grid>
        </Border>

        <!-- Room List -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Table Header -->
                <Border Grid.Row="0" 
                      Background="{StaticResource PrimaryColor}"
                      CornerRadius="4,4,0,0"
                      Padding="15,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="Room #" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Grid.Column="1" Text="Room Type" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Grid.Column="2" Text="Price" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Grid.Column="3" Text="Status" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Grid.Column="4" Text="Floor" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Grid.Column="5" Text="Actions" FontWeight="Bold" Foreground="White"/>
                    </Grid>
                </Border>

                <!-- Room DataGrid -->
                <DataGrid Grid.Row="1" 
                        ItemsSource="{Binding Rooms}"
                        Style="{StaticResource ModernDataGrid}"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        SelectionMode="Single"
                        SelectionUnit="FullRow"
                        SelectedItem="{Binding SelectedRoom}">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}"/>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Room #" Binding="{Binding RoomNumber}" Width="80"/>
                        <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType.RoomTypeName}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding RoomPricePerDate, StringFormat=\{0:C\}}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding RoomStatus}" Width="100"/>
                        <DataGridTextColumn Header="Floor" Binding="{Binding RoomFloor}" Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" 
                                              Style="{StaticResource ModernButton}"
                                              Background="{StaticResource InfoColor}"
                                              Padding="8,4"
                                              Margin="0,0,5,0"
                                              Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"/>
                                        <Button Content="Delete" 
                                              Style="{StaticResource ModernButton}"
                                              Background="{StaticResource AccentColor}"
                                              Padding="8,4"
                                              Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl> 