<Window x:Class="TranTuanKietWPF.BookingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
        </Style>
        
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
        </Style>
        
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#95a5a6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7f8c8d"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{Binding DialogTitle}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Customer Selection -->
                <TextBlock Text="Customer:" Margin="0,5,0,0"/>
                <ComboBox ItemsSource="{Binding Customers}" DisplayMemberPath="CustomerFullName" 
                          SelectedValuePath="CustomerID" SelectedValue="{Binding Booking.CustomerID}" 
                          Style="{StaticResource ComboBoxStyle}"/>
                
                <!-- Room Selection -->
                <TextBlock Text="Room:" Margin="0,5,0,0"/>
                <ComboBox ItemsSource="{Binding AvailableRooms}" 
                          SelectedValuePath="RoomID" SelectedValue="{Binding Booking.RoomID}" 
                          Style="{StaticResource ComboBoxStyle}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding RoomNumber}" FontWeight="Bold"/>
                                <TextBlock Text=" - " Margin="5,0"/>
                                <TextBlock Text="{Binding RoomType.RoomTypeName}"/>
                                <TextBlock Text=" ($" Margin="5,0"/>
                                <TextBlock Text="{Binding RoomPricePerDate, StringFormat=N2}"/>
                                <TextBlock Text="/night)"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <!-- Date Selection -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Check-in Date:" Margin="0,5,0,0"/>
                        <DatePicker SelectedDate="{Binding Booking.StartDate}" Style="{StaticResource DatePickerStyle}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Check-out Date:" Margin="0,5,0,0"/>
                        <DatePicker SelectedDate="{Binding Booking.EndDate}" Style="{StaticResource DatePickerStyle}"/>
                    </StackPanel>
                </Grid>
                
                <!-- Booking Type -->
                <TextBlock Text="Booking Type:" Margin="0,5,0,0"/>
                <ComboBox SelectedIndex="{Binding Booking.BookingType, Converter={StaticResource BookingTypeToIndexConverter}}" 
                          Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Online"/>
                    <ComboBoxItem Content="Offline"/>
                </ComboBox>
                
                <!-- Booking Status (only for editing) -->
                <TextBlock Text="Booking Status:" Margin="0,5,0,0" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <ComboBox SelectedIndex="{Binding Booking.BookingStatus, Converter={StaticResource BookingStatusToIndexConverter}}" 
                          Style="{StaticResource ComboBoxStyle}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ComboBoxItem Content="Active"/>
                    <ComboBoxItem Content="Completed"/>
                    <ComboBoxItem Content="Cancelled"/>
                </ComboBox>
                
                <!-- Price Information -->
                <Border Background="#f8f9fa" BorderBrush="#ddd" BorderThickness="1" CornerRadius="5" Margin="0,15,0,0" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Booking Summary" FontWeight="Bold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Duration:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Booking.BookingDuration}" Margin="10,0,0,0"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Price per night:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PricePerNight, StringFormat=C}" Margin="10,0,0,0"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Total Price:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Booking.TotalPrice, StringFormat=C}" FontWeight="Bold" Margin="10,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,10"/>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource CancelButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window> 